<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adolf.opencvstudy">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_opencv"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_opencv"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.CameraXActivity"></activity>
        <activity android:name=".ui.ShowProcessActivity" />
        <activity android:name=".ui.IdentifyFeaturesActivity" />
        <activity android:name=".ui.BlurSharpenActivity" />
        <activity android:name=".ui.MorphologyActivity" />
        <activity android:name=".ui.BinaryActivity" />
        <activity android:name=".ui.JavaCameraViewActivity" />
        <activity
            android:name=".AutoCropperActivity"
            android:configChanges="keyboardHidden"
            android:excludeFromRecents="true"
            android:exported="true"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>